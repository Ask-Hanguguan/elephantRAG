# ============================================================
# elephantRAG -- Dependencies
# ============================================================

# ---- Web UI ----
streamlit>=1.28.0
httpx>=0.27.0

# ---- REST API ----
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# ---- LLM & Agent Framework ----
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langchain-chroma>=0.2.0
langgraph>=0.2.0
langchain-openai>=0.3.0

# ---- DashScope SDK (LLM) ----
dashscope>=1.20.0

# ---- PDF 解析：PyMuPDF (高性能文字提取 + 嵌入图片渲染) ----
PyMuPDF>=1.24.0

# ---- OCR：PaddleOCR 3.x (中英文混合识别，CPU 友好) ----
# PaddlePaddle CPU 版 → PaddleOCR 3.x 会自动依赖
paddlepaddle==2.6.2
paddleocr==2.7.3

# ---- BM25 Retrieval ----
rank-bm25>=0.2.0
jieba>=0.42.0

# ---- Data Processing ----
pandas>=2.0.0
pydantic>=2.0.0
numpy==1.26.4

# ---- Utilities ----
pyyaml>=6.0

# ---- Document format readers (Office 轻量回退) ----
python-docx>=1.1.0
python-pptx>=1.0.0
openpyxl>=3.1.0
